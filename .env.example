# Production canonical domain — required for correct canonical tags, sitemap URLs,
# Open Graph tags, and JSON-LD before deploying. Falls back to http://localhost:3000 in dev.
NEXT_PUBLIC_SITE_URL=https://www.example.com
